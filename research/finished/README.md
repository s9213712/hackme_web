# Completed Implementation Notes

This directory stores instruction or recommendation files after their requested
scope has been implemented and verified.

Current status:

- `hackme_web_snapshot_superweak_agent_instructions.md`: completed by the
  snapshot/restore, server mode, superweak rollback, reset, and audit-backed
  workflow implemented in Phase 4 and Phase 5.

- `hackme_web_privacy_antivirus_upload_agent_instructions.md`: completed by the
  privacy upload policy, storage path validation, quota APIs, Cloud Drive safety
  UI, local ClamAV/magic/zip scanning pipeline, attachment references, download
  permission checks, and `/api/files/*` compatibility upload/status/download/
  E2EE share/revoke APIs implemented through Phase 11.
