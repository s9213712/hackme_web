# External ComfyUI integration dependencies.
#
# Connecting hackme_web to an already-running ComfyUI API server, such as
# http://127.0.0.1:8188, is covered by requirements-minimal.txt. The needed
# websocket-client package is intentionally part of the minimal runtime because
# the ComfyUI route module imports the external API client during app startup.
#
# Keep this file as a selectable deployment layer, but do not add local model
# runtime stacks here. Local Hugging Face / Diffusers inference belongs in
# requirements-hf.txt because it pulls torch/CUDA-scale dependencies.
