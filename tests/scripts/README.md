# Script Test Suites

Use this folder for tests that primarily guard script, CLI, or operator runner
contracts.

Examples:

- deploy and production launch scripts
- smoke / pentest / stress wrappers
- probe scripts
- offline recovery CLI
