# 2026-05-24 15:21 +0800 `:5000` continuous QA

## Result

- No confirmed product defect in this pass.
- The running `:5000` instance is serving the same checked key files as the repo copy for `public/index.html`, `public/js/00-core.js`, `public/js/56-trading.js`, `public/js/57-platform-centers.js`, `server.py`, and `test_for_develop.sh`.
- Final health check passed: `GET https://127.0.0.1:5000/api/healthz` returned HTTP 200 with `ok=true`.
- The sidebar regrouping request is intentionally still pending user confirmation; no sidebar code was changed in this pass.

## Live Instance

- Master PID: `1859888`.
- Workers: `1859947`, `1859948`, `1859949`, `1859950`.
- CWD: `/tmp/hackme_web_dev_20260524_1412_perf_4x6/hackme_web`.
- Gunicorn mode: `gthread`, `workers=4`, `threads=6`, HTTPS on `127.0.0.1:5000`.

## Coverage

- Member probe artifact: `/tmp/hackme_web_qa_5000_20260524/member_probe.json`.
  - 17 checks, 0 failures.
  - Covered root/member login, cloud drive upload/preview/share/E2EE negative paths, remote download safeguards, video/HLS/password flow, album sharing, grid fee math, and reserve allocation paths.
- Chat/video share link probe artifact: `/tmp/hackme_web_qa_5000_20260524/chat_video_share_link_probe.json`.
  - Passed.
  - Verified a `/shared/videos/...#vk=...` URL remains intact when rendered inside chat messages.
- PointsChain post-stress Playwright artifact: `/tmp/hackme_web_qa_5000_20260524/points_chain_post_stress.json`.
  - Passed.
  - Root wallet/report/transactions/fee-estimate APIs returned 200.
  - Member wallet/transactions/notifications APIs returned 200.
  - Chain UI showed `完整`; root report showed no chain verification errors.
- Trading background correctness artifacts:
  - `/tmp/hackme_web_qa_5000_20260524/trading_background_correctness_run2.json/trading_background_correctness.json`
  - `/tmp/hackme_web_qa_5000_20260524/trading_background_correctness_run2.json/trading_background_correctness.md`
  - 57 checks, 0 failures, `trigger_mode=auto`.
  - Confirmed server-owned background jobs ran without active member/root browser sessions.
  - Covered limit order matching, spot stop-loss, spot take-profit, workflow/conditional bot, DCA bot, grid bot, margin interest accrual, margin take-profit, margin liquidation, concurrent order stress, root background UI, `/api/root/trading/verify`, and `/api/root/points/chain/verify`.
  - Reserve pool remained non-negative: before `5,000,000`, after `5,000,000`.

## False Starts

- First chat/video share probe used `test/test` to modify `/api/admin/settings`; the 403 response was correct because only root/super-admin may change system settings. Re-run with `root/root` passed.
- First PointsChain post-stress run used the wrong argument/credential combination: default member username `admin` with password `test`. Re-run with `--member-username test --member-password test` passed.
- First trading Playwright run was launched inside the restricted sandbox and stalled before producing checks. It was killed and re-run with Playwright escalation; the escalated run passed.

## Logs

- `runtime/logs/server.log` produced no new tail output during the final check.
- Worker PIDs remained stable after the probes.
- CPU and memory usage after the run were normal for the live workers; the busiest worker was around 9% CPU and 1% memory at the sampling point.

## Follow-Up

- Continue normal/malicious multi-account QA on `:5000`.
- Wait for explicit confirmation before applying the proposed sidebar regrouping into `社交 / 功能 / 帳務 / 管理`.
