# 2026-05-26 17:52 :5000 Script Gate + Live QA

## Findings

- Resolved: `scripts/security/pentest/run_functional_smoke.sh` still treated `/api/root/points/chain/backups` as a successful 200 path. That endpoint is intentionally disabled because ledger backup/restore mutates accounting history; the smoke now expects 410 and asserts the disabled policy payload.
- Resolved: active QA probes still submitted trading examples as `ETH/POINTS`, `BTC/POINTS`, or `XRP/POINTS`. The backend may keep `*/POINTS` as an internal compatibility key, but user-facing QA probes now submit `*/USDT`.
- Resolved: `POST /api/root/trading/markets/<symbol>` accepted internal `*/POINTS` keys but rejected display symbols like `BTC/USDT` and `ETH/USDT` with `market not found`. The service now normalizes display symbols before updating runtime market settings.
- Resolved: `scripts/trading/validation/trading_exchange_validation.py` still expected block sealing to create a restorable ledger backup and did not fail when `verify_chain()` returned `financial_ok=false`. The validation now keeps restorable backups disabled, funds test users through pc0 official-hot wallet accounting, burns artificial liquidation-spend test debits explicitly, and requires `financial_ok=true`.

## Coverage

- Unit/script checks:
  - `bash -n scripts/security/pentest/run_functional_smoke.sh`
  - `python3 -m py_compile scripts/testing/predeploy_capacity_probe.py scripts/testing/system_stress_probe.py`
  - `pytest tests/scripts/security/test_functional_smoke_script.py tests/scripts/deploy/test_predeploy_capacity_probe.py tests/scripts/testing/test_system_stress_probe.py -q`
  - Result: `21 passed`.
- Trading validation:
  - `python3 scripts/trading/validation/trading_exchange_validation.py --json`
  - Result: `ok=true`, `21 passed`, including `financial_ok=true` and disabled ledger-backup policy.
- Skill probe:
  - `python3 /home/s92137/.codex/skills/hackme-web-qa/scripts/member_probe.py --base-url https://127.0.0.1:5000 ...`
  - Result file: `/tmp/hackme_live_member_probe_20260526_qa_after_script_fix/member_probe.json`
  - Result: `findings=0`, and the probe report no longer contains `ETH/POINTS`, `BTC/POINTS`, or `XRP/POINTS`.
- Live API checks on `https://127.0.0.1:5000`:
  - root login succeeded.
  - `/api/root/points/chain/backups` returned 410 with `disabled=true`.
  - `/api/root/points/chain/verify` returned `ok=true`, `verification_ok=true`, `financial_ok=true`, `invariant_ok=true`, `error_count=0`.
  - `/api/trading/live-price?market=ETH/USDT` returned 200 with `display_market_symbol=ETH/USDT`.
  - `/api/trading/grid/preview` with `market_symbol=ETH/USDT` returned 200.
- Mixed live stress:
  - Result file: `/tmp/hackme_live_system_stress_20260526_after_script_fix.json`
  - 60 logical users, 180 requested ops, concurrency 12.
  - Result: `ok=true`, `degraded=false`, no 5xx, no `server_busy`.
  - Latency: p95 `520.811ms`, p99 `652.143ms`, max `889.808ms`.
  - Resource: CPU avg `22.11%`, CPU max `25.29%`, monitored RSS max `532.54MB`.
  - `trading_grid_preview`: 2/2 HTTP 200, p95 `383.719ms`.
- Browser/live trading follow-up:
  - `python3 scripts/testing/points_chain_post_stress_playwright.py --base-url https://127.0.0.1:5000 ...`
  - Result: `browser_errors=[]`, root chain status visible as `完整`, `全鏈驗證正常：703 筆 ledger，1 個封塊，4 筆未封，944 筆審計事件`.
  - `python3 scripts/security/pentest/functional_permission_pentest.py --base-url https://127.0.0.1:5000 ... --core-only`
  - First run found one stale expectation: disabled recovery apply returned 410 while the pentest still expected missing-confirm 400. After script update: `87 passed`, `0 failed`.
  - `python3 scripts/security/pentest/trading_stress_pentest.py --base-url https://127.0.0.1:5000 --mode permission_probe ...`
  - First run reproduced the market path bug: `BTC/USDT` and `ETH/USDT` market config returned 400 `market not found`. After backend route normalization and HUP reload: `failures=0`, `requests=17`.
  - Post-fix mixed live stress: `/tmp/hackme_live_system_stress_20260526_after_usdt_route_fix.json`, 50 logical users, 150 requested ops, concurrency 10, `ok=true`, `degraded=false`, no 5xx, no `server_busy`, p95 `457.992ms`, p99 `519.087ms`, max `1524.249ms` on expected bad-login path, CPU max `26.17%`, monitored RSS max `393.6MB`.
  - Post-fix PointsChain verification: `financial_ok=true`, `invariant_ok=true`, `error_count=0`.

## Notes

- A server HUP reload was required after `services/trading/admin.py` was copied into the live `/tmp` instance. New workers were observed after reload.
- `gunicorn_error.log` still contains local TLS certificate warnings from automated probes, but no new hard 5xx was appended to `backpressure_anomalies.jsonl` during this run.
